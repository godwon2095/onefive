<% if current_user.is_follower?(@user) %>
  $('#follow').text("구독취소");
<% else %>
  $('#follow').text("구독하기");
<% end %>
  $('#followers-size').text("<%= @user.followers.size %>");
  $('#followings-size').text("<%= @user.followings.size %>");
