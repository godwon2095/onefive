<% if current_user.is_follower?(@user) %>
  $('#follow').text("구독취소");
<% else %>
  $('#follow').text("구독하기");
<% end %>
$('#followings-length').text("<%= @user.followings.size %>");
$('#followers-length').text("<%= @user.followers.size %>");
